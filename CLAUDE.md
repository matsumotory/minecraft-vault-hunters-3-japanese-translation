# CLAUDE.md vault-hunters-3-japanese-translation

このリポジトリはpublic前提。規律は本ファイルに自己完結で持ち、外部の非公開リポジトリの参照で済ませない。新しいセッションは本ファイル全文とHANDOFF.mdを読んでから着手する。

## 1. このリポジトリは何か

Minecraftのmodpack「Vault Hunters 3rd Edition」(Minecraft 1.18.2、Forge) を日本語で遊べるようにする翻訳companion mod。lang層とゲーム内ガイドブックの訳文をmodのassetsとして同梱し、config層の訳文をゲーム起動時に手元のconfigへ差し込み、ハードコード層の置換設定をVaultPatcher (別mod、Required Library) へ供給する。翻訳そのもの (白紙翻訳、監査、用語決め) は別の作業環境で行われ、このリポジトリには生成済みの訳文データだけが入る。

対象はMinecraft Java版1.18.2、ローダーはForge (ForgeGradle 5 + Java 17)。modidはvhjapanese。配布はCurseForgeとGitHub Releases。ライセンスはmatsumotoryの創作部分がMIT。

## 2. 権利の規律 (最重要)

- Vault Huntersとthe_vaultはTeam Iskallia (85 Entertainment AB) の著作物 (All Rights Reserved)。原作の複製 (jar、mod本体、config原文の丸ごと、英語原文のコーパス) を、このリポジトリにも配布物にも一切含めない
- 含めてよいのは訳文と、機能上不可欠な最小限の参照 (langキー名、configのJSONパス、置換対象を特定するための原文文字列) だけ
- 非公式・非収益のファンプロジェクト。権利者から要請があれば争わず配布を取り下げる
- 日本語訳の一部はMITライセンスの先行翻訳 (suzu2469/vault_hunter_lang_jp) を土台にしている。帰属はNOTICE.mdが正
- 上流PRや許諾の問い合わせを開発の経路に置かない。他者の返答を出荷の条件にしない

## 3. 開発の規律

- 壊さないことが最優先。configへの差し込みは必ず元ファイルのバックアップを先に取り、JSONの構文検証を通ったものだけを書き、いつでも復元できる形にする。翻訳が無い文字列は英語のまま表示されるのが正しい挙動で、欠けを埋めるために構造を壊さない
- 動くと確かめてから動いたと言う。ゲームを実際に起動して確認し、確認した手順を報告とコミット本文に書く
- 外部の技術仕様 (Forge、VaultPatcher、CurseForge) は版で変わる。訓練データの記憶で設計せず、公式ドキュメントと実物の実文を確認してから書く
- このmodは翻訳の適用という1つの機能だけを持つ。ゲーム性に触れる機能は作らない
- 低スペックのマシンでも動く軽さを保つ。処理は起動時と再読込時に限り、常時のティック処理と描画フックを持たない
- 対象modpackバージョンを常に明示する。modのバージョンは「対象modpackバージョン-ja.通し番号」(例 3.21.7-ja.1)。対象が上がれば前半を上げ、同一対象への訳の改善は通し番号だけ上げる
- 文字コードの罠: the_vaultはconfigをJVM既定文字コードで読む (日本語WindowsではCP932)。配布するJSONの日本語はASCIIエスケープで書く

## 4. 公開基準 (このリポジトリはpublic)

- trackedファイルとコミット履歴に、個人情報、ローカルの絶対パス、他の非公開プロジェクトの名前、内部の作業環境の詳細を書かない。翻訳の作業環境に触れるときは「別の作業環境」の一般形で書く
- 文書 (.md) にemdashとendash (U+2014、U+2013) を使わない (機械生成臭のする記号を公開文面に残さない。check_publish_safety.pyが門。同梱の翻訳データは対象外)
- 利用者向けの文書 (README、NOTICE、ストア説明文、CHANGELOG、リリースノート) は、敬体 (です・ます) でプレイヤーの目線で書く。数字を並べるときは、分類の意味と割合を言葉で説明し、読者に計算や推測をさせない。作者を指す語は「当方」を使わず、アカウント名のmatsumotoryで書く (初出に読みの「まつもとりー」を添える。先行翻訳をsuzu2469とアカウント名で呼ぶのと同じ基準)。主語と述語のそろった能動の文で、行間を読ませず具体的に書く (2026-08-16の本人の指示)。内部の運用文書 (本ファイル、HANDOFF) は簡潔な常体でよい
- コミット前に必ず`python scripts/check_publish_safety.py && git commit`の形で機械検査を&&で直結する。素のgrepを門に使わない (grepはヒットで終了コード0を返すため、&&直結だと見つかったのに通る逆向きの門になる)
- 検査に足す内部の固有名は、tracked のスクリプトではなくgit管理外のprivate-patterns.local.txtへ書く (禁止リスト自体を公開しない設計)
- 公開範囲を変える操作 (public切替) の前に、`python scripts/check_publish_safety.py --history`で全コミット履歴も検査する
- コミットメッセージは日本語で厚く書く。何を変えたかに加えて、なぜか、何を確かめたかを書く。公開基準が厚さに優先する
- READMEは日本語 (README.md) と英語 (README.en.md) の両方を持ち、冒頭で相互にリンクする。権利・帰属・取り下げの内容は日英で同等に読めるようにし、NOTICEは日英併記にする。英語側には権利者向けの連絡先の節 (For the rights holder) を必ず置く (権利者が読む場所と導線を英語で用意する。2026-08-17の本人の指示)

## 5. 作業の仕方

- セッションの作業は`.claude/worktrees/`配下の隔離worktreeで行い、主たる作業コピーを直接書き換えない。参照の前に`git fetch origin main`する
- 文書は現在形に保ち、履歴はgitに任せる。作業状態はHANDOFF.mdへ書く。決まった方針はその場で本ファイルへ焼き、文脈を会話に置き去りにしない
- 本人にしか決められないこと (アカウントの操作、費用、公開の判断) だけ確認し、それ以外は推奨を添えて自走する
